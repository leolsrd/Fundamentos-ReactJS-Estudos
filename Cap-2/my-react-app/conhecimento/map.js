// ? Utilizando map
// ? O map ele não altera o lista/ array original e sim cria uma nova
// ? lista/ array como resultado

const number = [1, 2, 3, 4, 5];
const doubled = number.map((num) => num * 2);
console.log(doubled);
